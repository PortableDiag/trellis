/target

# Trellis documents may contain private notes/credentials — never commit them.
*.ron

# Dev session reports are private — never commit them to this public repo.
docs/SESSION-*.md

# A plugin's own credentials live beside it and must never be committed.
plugins/*/config.json
plugins/*/state.json

# Python bytecode from the bundled plugins
__pycache__/
*.pyc
